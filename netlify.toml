[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200

# Gym reminder — runs every minute, sends SMS when clock matches GYM_REMINDER_TIMES
[functions."gym-reminder"]
  schedule = "* * * * *"
